# Fase 1B uses only the Python standard library plus system FFmpeg/ffprobe.
# No mandatory Python dependencies yet.

# Torch CPU should be installed later with the official CPU-specific command
# for the target Python version and platform. Do not pin aggressively yet.
# Example placeholder only:
# pip install torch torchaudio --index-url https://download.pytorch.org/whl/cpu
